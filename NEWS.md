# safesink 0.2.0
* Added row-wise fit scoring: `cwtvd_decompose()`, `rowwise_score()`,
  `rowwise_rel_improvement()` for per-occupation relative fit improvement,
  with conditional (volume-independent) normalization.