% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sinkhorn_aligned.R
\name{sinkhorn_aligned}
\alias{sinkhorn_aligned}
\title{Align marginals with cost matrix before solving OT}
\usage{
sinkhorn_aligned(a, b, C, epsilon, solver, ...)
}
\arguments{
\item{a}{Named numeric vector representing the origin marginal distribution.}

\item{b}{Named numeric vector representing the destination marginal distribution.}

\item{C}{Cost matrix whose row and column names correspond to the elements of
\code{a} and \code{b}.}

\item{epsilon}{Entropy regularization parameter passed to the solver.}

\item{solver}{Function implementing a Sinkhorn solver. The function must
accept arguments \verb{(a, b, C, epsilon, ...)} and return a list containing at
least a transport matrix \code{plan}.}

\item{...}{Additional arguments passed to the solver.}
}
\value{
The result returned by the specified solver.
}
\description{
Ensures that the origin and destination marginals are correctly aligned
with the rows and columns of the cost matrix before calling a Sinkhorn
solver. This prevents subtle errors caused by mismatched ordering of
marginals and the cost matrix.
}
\details{
The function checks that the names of \code{a} match the row names of \code{C}
and that the names of \code{b} match the column names of \code{C}. The cost
matrix is then reordered to match the ordering of the marginals before
passing the inputs to the specified solver.

This wrapper is useful when working with named marginals, where the
ordering of elements may not match the ordering of the cost matrix.
}
